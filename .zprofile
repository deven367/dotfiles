# ~/.zprofile — login shells only.
#
# Shared env (PATH, keys, modules) lives in ~/.config/zsh/env.zsh, sourced
# by ~/.zshenv for every zsh. lair has no Homebrew, so nothing
# login-specific to set up here.
